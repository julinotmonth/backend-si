# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=si-dirok-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5000
DB_NAME=sidirok_db
DB_USER=postgres
DB_PASSWORD=jeje1290

# Alternative: Use DATABASE_URL (for cloud deployment like Railway, Render, etc.)
# DATABASE_URL=postgresql://username:password@host:port/database

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
